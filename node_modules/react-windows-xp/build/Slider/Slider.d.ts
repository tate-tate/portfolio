import React, { FunctionComponent } from "react";
export interface SliderProps extends Omit<React.DetailedHTMLProps<React.InputHTMLAttributes<HTMLInputElement>, HTMLInputElement>, "onChange"> {
    id: string;
    min: number;
    max: number;
    onChange?: (value: number) => void;
    label?: React.ReactNode;
    vertical?: boolean;
    boxIndicator?: boolean;
}
export declare const Slider: FunctionComponent<SliderProps>;
